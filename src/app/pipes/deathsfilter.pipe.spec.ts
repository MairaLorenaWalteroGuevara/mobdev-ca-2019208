import { DeathsfilterPipe } from './deathsfilter.pipe';

describe('DeathsfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new DeathsfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
